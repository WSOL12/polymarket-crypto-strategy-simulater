export function StrategySimPage() {
  return (
    <main className="container">
      <h1 className="pageTitle">Strategy simulation</h1>
      <p className="pageLead">
        Backtest or forward-test your own rules (entries, sizing, hedges). Wire parameters and results tables here.
      </p>
      <section className="panel">
        <p className="muted">Placeholder — connect your strategy engine and reporting.</p>
      </section>
    </main>
  );
}
